<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View for Estate Property -->
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Estate Properties">
                <field name="name"/> <!-- Property name -->
                <field name="expected_price"/> <!-- Expected price -->
                <field name="selling_price"/> <!-- Selling price -->
                <field name="state"/> <!-- Property state -->
                <field name="living_area"/> <!-- Living area -->
                <field name="total_area"/> <!-- Total area -->
                <field name="price_per_sqm"/> <!-- Price per square meter -->
                <field name="bedrooms"/> <!-- Number of bedrooms -->
                <field name="garden"/> <!-- Garden availability -->
                <field name="garage"/> <!-- Garage availability -->
                <field name="active"/> <!-- Active status -->
            </list>
        </field>
    </record>

    <!-- Form View for Estate Property -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="available_from"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area"/>
                                <field name="garden_orientation"/>
                            </group>
                        </page>
                        <page string="Area &amp; Pricing">
                            <group>
                                <field name="total_area" readonly="1"/>
                                <field name="price_per_sqm" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <sheet>
                    <group>
                        <group>
                            <group>
                                <field name="postcode"/>
                                <field name="active"/>
                                <field name="state"/>
                            </group>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Define an Action for the Views -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_estate_property_list"/>
    </record>

</odoo>
